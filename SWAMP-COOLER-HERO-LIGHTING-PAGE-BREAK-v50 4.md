# Swamp Cooler Hero Lighting + Page Break — v50

This follow-up keeps all v49 work and makes only the two requested presentation changes on `/services/swamp-coolers`.

## Hero
- Removed the swamp-cooler-specific right-side darkening layer.
- The existing left-side legibility gradients remain unchanged, so headline contrast is preserved.
- The right side now shows the rooftop house image more clearly.

## Lower page-break image
- Kept the existing uploaded airflow page-break asset.
- Changed its object position to `center top` so the top of the cooler remains visible instead of being cropped away by the fixed-height section.

## Updated file
- `src/pages/services/SwampCoolerServices.jsx`
