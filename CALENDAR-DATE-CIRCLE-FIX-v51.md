# Calendar Date Circle Fix — v51

This build updates the date buttons in the quote-form calendar so each day renders as a true circle instead of stretching into a pill shape.

## File updated
- `src/components/ui/QuoteForm.jsx`

## Change made
- Added a fixed width and centered each calendar day button inside its grid cell.
- The selected day state now displays as a perfect circle.

All prior v50 changes remain included.
